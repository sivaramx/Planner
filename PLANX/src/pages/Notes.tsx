import {useState} from "react";
import {NotebookPen,Plus,Pin} from "lucide-react";
import {usePlanxStore} from "../store/usePlanxStore";
export function Notes(){const {notes,addNote}=usePlanxStore(),[title,setTitle]=useState(""),[content,setContent]=useState(""),create=()=>{if(title.trim()){addNote({title:title.trim(),content,tags:[],pinned:false,archived:false});setTitle("");setContent("")}};return <div className="page"><header className="page-header"><div><h1>Notes</h1><p>Keep useful context beside your work.</p></div></header><div className="note-create"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Note title"/><textarea value={content} onChange={e=>setContent(e.target.value)} placeholder="Write your note..." rows={3}/><button className="primary" onClick={create}><Plus size={15}/> Save note</button></div><div className="note-grid">{notes.filter(n=>!n.archived).map(n=><article className="note-card" key={n.id}><div><NotebookPen size={16}/>{n.pinned&&<Pin size={13}/>}</div><h2>{n.title}</h2><p>{n.content||"Empty note."}</p></article>)}</div></div>}
