import {usePlanxStore} from "../store/usePlanxStore";
export function Archive(){const {tasks,updateTask}=usePlanxStore();const archived=tasks.filter(t=>t.archived);return <div className="page"><header className="page-header"><div><h1>Archive</h1><p>Keep old work out of your active workspace.</p></div></header><div className="archive-list">{archived.map(t=><div className="archive-row" key={t.id}><span>{t.title}</span><button className="secondary" onClick={()=>updateTask(t.id,{archived:false})}>Restore</button></div>)}{!archived.length&&<div className="empty"><h3>Archive is empty.</h3></div>}</div></div>}
